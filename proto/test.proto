syntax = "proto3";

package test;

service TestService {
  rpc MakePing(Ping) returns (PingResponse) {}
}

message Ping {
  int32 id = 1;
  string message = 2;
}

message PingResponse {
  int32 status = 1;
  string message = 2;
}